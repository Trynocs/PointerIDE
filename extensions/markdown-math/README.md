# Markdown Math

**Notice:** This extension is bundled with Pointer. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to Pointer's built-in markdown preview and markdown cells in notebooks.
